export type SignInProps = {
    email: string;
    password: string;
};

export type SignUpProps = {
    name: string;
    email: string;
    password: string;
    checkbox: boolean;
    phoneNumber: string;  
};